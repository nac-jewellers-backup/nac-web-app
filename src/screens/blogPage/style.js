import { makeStyles } from "@material-ui/core/styles";
const BlogPageStyles = makeStyles((theme) => ({
  viewMore: {
    textAlign: "center",
    marginBottom: "20px",
    "& button": {
      backgroundColor: "#393a96",
      color: "#ffffff",
    },
  },
}));
export default BlogPageStyles;
