import { makeStyles } from "@material-ui/core/styles";
const ExperienceBannerStyles = makeStyles((theme) => ({
  bannerImage: {
    "& img": {
      width: "100%",
    },
  },
  
  // MOBILE CSS

  mobileBanner: {
    "& img": {
      width: "100%",
    },
  },

  
}));
export default ExperienceBannerStyles;
